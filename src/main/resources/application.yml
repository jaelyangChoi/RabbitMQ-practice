spring:
  rabbitmq:
    host: localhost
    port: 5672 # ?? ?? ??, 15672? ?? ?? ? ????(admin) ?
    username: guestuser
    password: guestuser
  application:
    name: HelloMessageQueue
server:
  port: 8080
